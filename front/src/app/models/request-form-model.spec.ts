import { RequestFormModel } from './request-form-model';

describe('RequestFormModel', () => {
    it('should create an instance', () => {
        expect(new RequestFormModel()).toBeTruthy();
    });
});
